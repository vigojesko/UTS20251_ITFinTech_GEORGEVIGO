/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "monodramatic-indefinably-skyla.ngrok-free.dev"],
  },
};

export default nextConfig;